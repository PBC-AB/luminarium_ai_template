import { ExamplePage } from "@/features/example-feature/components/ExamplePage";

export default function Home() {
  // For now, just render the example page at the root route.
  // This can be replaced with actual routing or a landing page later.
  return <ExamplePage />;
}
